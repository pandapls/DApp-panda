import puppeteer from 'puppeteer';
import Infrastructure from './infrastructure';
export default class Basic extends Infrastructure {
    sleep(ms: number): this;
    execute(fn: (this: Infrastructure, browser: puppeteer.Browser, page: puppeteer.Page) => void): this;
    end(): Promise<void>;
    end(callback: () => void): void;
    awaitPromise(): Promise<void>;
}
